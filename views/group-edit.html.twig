{% extends "layout.html.twig" %}

{% block content %}
<form action="{{baseUrl}}/group{% if groupname is defined %}/{{ groupname }}{% endif %}/{{ action }}" class="form-horizontal" method="post">
  {% if action == 'add' %}
    {{ form_row(form.name) }}
  {% endif %}
  {{ form_row(form.users) }}

  <hr/>
  <button type="submit" class="btn btn-primary">Save</button>
  <a href="{{baseUrl}}/" class="btn btn-link">Back to List</a>

</form>
{% endblock %}
